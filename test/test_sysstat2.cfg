/*
 *		システム状態に関するテスト(2)のシステムコンフィギュレーションファイル
 *
 *  $Id: test_sysstat2.cfg 898 2022-06-21 12:30:39Z ertl-hiro $
 */
INCLUDE("test_common1.cfg");

#include "test_sysstat2.h"

DEF_SCY({ 40 * TEST_TIME_CP });
CRE_SOM(SOM1, { TA_INISOM });

CLASS(CLS_PRC1) {
  ATT_TWD({ DOM1, SOM1, 1, 30 * TEST_TIME_CP });

  DOMAIN(DOM1) {
	CRE_TSK(TASK11, { TA_ACT, 1, task11, MID_PRIORITY, STACK_SIZE, NULL });
	SAC_TSK(TASK11, { TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
	CRE_TSK(TASK12, { TA_NULL, 1, task12, HIGH_PRIORITY, STACK_SIZE, NULL });
	SAC_TSK(TASK12, { TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
  }
  KERNEL_DOMAIN {
	CRE_TSK(TASK21, { TA_NULL, 1, task21, MID_PRIORITY, STACK_SIZE, NULL });
	SAC_TSK(TASK21, { TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });

	SAC_SYS({ TACP(DOM1), TACP(DOM1), TACP_KERNEL, TACP_KERNEL },
			{ TACP(DOM1), TACP(DOM1), TACP_KERNEL, TACP_KERNEL });
  }
}
ATT_MOD("test_sysstat2.o");

INCLUDE("test_common2.cfg");
